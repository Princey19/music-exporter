{
  "name": "youtube-music-exporter",
  "version": "1.0.0",
  "description": "YouTube Music Exporter - find an artist's YouTube uploads missing from your company catalog",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
